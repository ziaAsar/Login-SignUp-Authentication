import 'package:flutter/material.dart';
class HomeAnas extends StatelessWidget {
  const HomeAnas({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.red,
      child: Text("Welcome to dashboard",style: TextStyle(color: Colors.black),),
    );
  }
}
